import 'package:flutter/material.dart';

class Details extends StatelessWidget {
  const Details(data, {super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
